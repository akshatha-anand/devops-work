pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name demo-stack --template-body file://s3.json --region 'us-east-1'"
              }
             }
            }
            }


