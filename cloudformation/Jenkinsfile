pipeline {
    agent any
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name demo-stack-1 --template-body file://cloudformation/s3.json --region 'us-east-1'"
              }
             }
            }
            }


